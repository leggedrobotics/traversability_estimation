# Footprint paths to check.
traversability_msgs/FootprintPath[] paths

---

# For each path if path is safe to traverse.
bool[] is_safe

# Estimate of the traversability for each path.
# Ranges from 0 to 1 where 0 means not traversable and 1 highly traversable.
float64[] traversabilities